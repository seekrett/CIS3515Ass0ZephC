abstract class Shape (_name : String) : Dimensionable {

    // variable declarations
    var name = _name

    // default function to get the area of a shape
    open fun getArea(): Double {
        return 0.0
    }

}
